# 날씨 앱 페이지 로드맵

이 문서는 앱에 필요한 전체 화면을 한눈에 확인하고 제작 순서를 결정하기 위한 사이트맵 겸 작업 기준입니다. 이 앱은 날씨에 맞는 놀거리, 먹거리, 볼거리 상품을 탐색하고 앱 안에서 구매·예약까지 완료하는 날씨 기반 커머스를 기준으로 합니다.

## 전체 페이지 사이트맵

```text
앱
├─ 시작
│  ├─ 스플래시
│  ├─ 앱 기능 소개
│  ├─ 위치 권한 안내
│  └─ 첫 사용 가이드
├─ 날씨
│  ├─ 날씨 메인
│  ├─ 시간별·주간 예보 상세
│  └─ 지역 검색 / 지역 관리
├─ 상품 탐색
│  ├─ 놀러 상품 목록
│  ├─ 먹으러 상품 목록
│  ├─ 보러 상품 목록
│  ├─ 전체 상품 / 통합 검색
│  └─ 상품 상세
├─ 구매
│  ├─ 장바구니
│  ├─ 주문서 / 결제
│  ├─ 주문 완료
│  ├─ 주문·예약 내역
│  ├─ 주문·예약 상세
│  └─ 취소·환불 신청
├─ 계정
│  ├─ 로그인
│  ├─ 회원가입
│  ├─ 아이디·비밀번호 찾기
│  ├─ 비밀번호 변경
│  └─ 마이페이지
├─ 내 활동
│  ├─ 찜 목록
│  ├─ 최근 본 항목
│  ├─ 쿠폰 / 포인트
│  └─ 알림
└─ 기타
   ├─ 설정
   ├─ 공지사항
   ├─ 자주 묻는 질문
   ├─ 문의하기
   ├─ 이용약관
   └─ 개인정보처리방침
```

## 전체 페이지 목록

| 분류   | 페이지                   | 경로 제안                 | 우선순위 | 역할                                            |
| ------ | ------------------------ | ------------------------- | -------- | ----------------------------------------------- |
| 시작   | 스플래시                 | `/`                       | 1차      | 브랜드 인트로와 앱 초기화                       |
| 시작   | 앱 기능 소개             | `/onboarding`             | 1차      | 날씨와 쇼핑 기능을 3~4장으로 소개               |
| 시작   | 위치 권한 안내           | `/onboarding/location`    | 1차      | 현재 위치 사용 이유와 대체 방법 안내            |
| 시작   | 첫 사용 가이드           | 화면 위 오버레이          | 1차      | 실제 화면의 주요 버튼과 구매 방법 안내          |
| 날씨   | 날씨 메인                | `/weather`                | 1차      | 현재 날씨, 대기질, 특보와 상품 추천 요약        |
| 날씨   | 예보 상세                | `/forecast`               | 1차      | 시간별·주간 예보 확인                           |
| 상품   | 놀러 상품 목록           | `/products/play`          | 1차      | 레저, 체험과 실내·야외 활동 상품 탐색           |
| 상품   | 먹으러 상품 목록         | `/products/eat`           | 1차      | 음식, 식사권과 맛집 예약 상품 탐색              |
| 상품   | 보러 상품 목록           | `/products/watch`         | 1차      | 전시, 공연, 영화와 관람권 상품 탐색             |
| 상품   | 쇼핑 메인·전체 상품 목록 | `/products`               | 구현 중  | `사러·먹으러·놀러` 탭과 상품 섹션 탐색          |
| 상품   | 상품 상세                | `/products/:productId`    | 1차      | 이미지, 가격, 옵션, 일정, 재고와 구매 정보 제공 |
| 검색   | 통합 검색                | `/search`                 | 2차      | 상품, 장소, 카테고리와 지역 검색                |
| 구매   | 장바구니                 | `/cart`                   | 1차      | 옵션과 수량을 선택한 상품 보관                  |
| 구매   | 주문서·결제              | `/checkout`               | 1차      | 주문자, 이용자, 쿠폰과 결제수단 입력            |
| 구매   | 주문 완료                | `/checkout/complete`      | 1차      | 결제 결과와 주문번호 확인                       |
| 구매   | 주문·예약 내역           | `/orders`                 | 2차      | 구매한 상품과 예약 상태 확인                    |
| 구매   | 주문·예약 상세           | `/orders/:orderId`        | 2차      | 이용 정보, 결제 금액과 상태 확인                |
| 구매   | 취소·환불                | `/orders/:orderId/cancel` | 2차      | 취소 가능 여부 확인과 환불 신청                 |
| 계정   | 로그인                   | `/login`                  | 2차      | 사용자 인증                                     |
| 계정   | 회원가입                 | `/signup`                 | 2차      | 새 계정 생성                                    |
| 계정   | 계정 찾기                | `/account-recovery`       | 2차      | 아이디·비밀번호 찾기와 재설정                   |
| 계정   | 비밀번호 변경            | `/password`               | 2차      | 로그인 사용자의 비밀번호 변경                   |
| 사용자 | 마이페이지               | `/mypage`                 | 2차      | 프로필과 내 활동 요약                           |
| 사용자 | 찜 목록                  | `/favorites`              | 2차      | 저장한 추천 항목 확인                           |
| 사용자 | 최근 본 항목             | `/recent`                 | 3차      | 최근 열어본 항목 확인                           |
| 사용자 | 쿠폰·포인트              | `/benefits`               | 3차      | 보유 혜택과 사용 내역 확인                      |
| 사용자 | 알림                     | `/notifications`          | 3차      | 날씨 특보와 서비스 알림 확인                    |
| 날씨   | 지역 관리                | `/locations`              | 2차      | 날씨를 볼 지역 검색·저장·전환                   |
| 기타   | 설정                     | `/settings`               | 2차      | 단위, 위치, 알림과 앱 설정 관리                 |
| 지원   | 공지사항                 | `/notices`                | 3차      | 서비스 공지 확인                                |
| 지원   | 고객지원                 | `/support`                | 3차      | FAQ와 문의 기능 제공                            |
| 정책   | 약관·개인정보            | `/legal`                  | 출시 전  | 이용약관, 개인정보와 취소·환불 정책 제공        |

## 제작 단계

먹으러 W 로고 마스크 크기는 커머스 토큰의 `100% auto`로 확대해 비주얼 안에서 더 크게 표시합니다.

### 1차: 비회원 핵심 경험

스플래시 → 온보딩 → 날씨 메인 → 놀러·먹으러·보러 상품 목록 → 상품 상세 흐름을 먼저 완성합니다. 목업 데이터만으로 상품 탐색과 옵션 선택이 가능해야 합니다.

### 2차: 구매 흐름

장바구니 → 주문서 → 결제 → 주문 완료 → 주문 상세 → 취소·환불 흐름을 연결합니다. 실제 결제 연동 전에는 테스트 결제 상태로 성공, 실패와 사용자 취소를 모두 검증합니다.

### 3차: 계정과 개인화

로그인, 회원가입, 계정 찾기, 마이페이지, 찜 목록, 주문 내역, 지역 관리와 설정을 추가합니다. 로그인을 하지 않아도 날씨와 상품은 볼 수 있지만 구매 시에는 로그인 또는 비회원 주문 정책이 필요합니다.

### 4차: 운영 기능

최근 본 항목, 쿠폰·포인트, 알림, 공지사항과 고객지원을 추가합니다.

## 첫 실행 온보딩과 사용 가이드

첫 실행 안내는 `기능 소개 화면`과 `화면 위 가이드`의 두 단계로 구성합니다.

### 1. 앱 기능 소개

스플래시 다음에 최초 한 번만 표시합니다. 한 화면에 설명을 몰아넣지 않고 아래 내용을 4장 이내로 나눕니다.

1. 오늘의 날씨와 시간별·주간 예보 확인
2. 날씨에 맞는 `놀러`, `먹으러`, `보러` 상품 탐색
3. 날짜, 회차와 인원을 선택해 장바구니에 담고 구매·예약
4. 마이페이지에서 주문, 예약, 찜과 쿠폰 확인

각 장에는 이미지 또는 캐릭터, 짧은 제목, 한두 줄 설명을 사용합니다. `다음`, `건너뛰기`, 마지막 장의 `시작하기` 버튼을 제공합니다.

### 2. 위치 권한 안내

운영체제의 권한 팝업을 바로 띄우지 않고 앱 화면에서 현재 위치가 필요한 이유를 먼저 설명합니다. `현재 위치 사용`, `지역 직접 선택`, `나중에 하기`를 제공합니다.

### 3. 첫 사용 가이드

날씨 메인에 처음 진입하면 화면 위 말풍선 또는 스포트라이트로 실제 UI를 순서대로 가리킵니다.

1. 현재 지역 변경 버튼
2. 시간별·주간 예보 영역
3. `놀러`, `먹으러`, `보러` 카테고리
4. 장바구니
5. 마이페이지

각 단계에는 `다음`, `이전`, `그만 보기`를 제공하며 마지막에는 `완료`를 표시합니다. 화면이 작거나 대상 요소가 보이지 않으면 해당 요소가 보이도록 먼저 스크롤합니다.

### 표시와 저장 규칙

- 기능 소개와 첫 사용 가이드는 각각 완료 여부를 기기에 저장해 매번 반복하지 않습니다.
- 앱 업데이트로 중요한 기능이 추가되면 해당 기능 안내만 새로 표시할 수 있도록 가이드 버전을 저장합니다.
- 사용자가 중간에 닫아도 앱은 정상적으로 사용할 수 있어야 합니다.
- `설정 > 앱 사용 가이드`에서 언제든 다시 실행할 수 있게 합니다.
- 화면 읽기 도구, 키보드 탐색과 모션 감소 설정을 지원합니다.
- 로그인 전에도 날씨와 상품 탐색 방법을 안내하되, 구매 과정의 상세 안내는 장바구니를 처음 사용할 때 보여줍니다.

## 핵심 커머스 페이지 상세 기준

### 상품 목록 페이지

- 쇼핑 메인의 상단 탭은 `사러`, `먹으러`, `놀러` 세 가지로 고정
- `사러`, `먹으러`, `놀러` 카테고리와 전체 상품 목록을 같은 상품 카드 시스템으로 구성
- 지역, 날짜, 가격, 실내·야외, 즉시 사용 가능 여부 필터 제공
- 추천순, 인기순, 낮은 가격순과 최신순 정렬 제공
- 품절, 판매 종료, 할인과 예약 가능 상태 표시
- 로딩, 검색 결과 없음과 마지막 페이지 상태 제공

현재 `ShoppingPage`에 아이폰 17 기준 `사러` 개별 화면의 전체 모바일 레이아웃을 구현했습니다. 상단 탭·검색, 날씨 질문, 대표 추천, 원형 연관 상품, 광고, 다이어리 목록, 판매처 탭 상품, 원형 대형 상품 슬라이드, 단일 배너 상품, 마스크 2열 목록과 고정 하단 내비게이션 순서입니다. 실제 이미지가 준비되기 전까지 각 이미지 비율에 맞춘 빈 플레이스홀더를 유지합니다. 판매처 원형 자리와 상품 슬라이드는 동적으로 생성하며, 판매처 탭을 선택하면 같은 섹션의 상품 콘텐츠가 변경됩니다.

상단 대표 추천은 3장 가로 스냅 슬라이드로 구현합니다. 터치 스크롤과 다음 추천 버튼을 지원하며 현재 슬라이드 번호를 `현재/전체` 형식으로 동기화합니다.

대표 추천 영역 아래에만 섹션 라인을 표시합니다. 이후 큐레이션 섹션에는 공통 하단 라인을 추가하지 않으며, 목록형 상품 내부의 행 구분선만 유지합니다.

광고 아래 모든 큐레이션 섹션 사이는 36 기준 `2.25rem` 간격으로 통일하며, 인접 섹션의 상·하 패딩이 중복되지 않게 단일 간격으로 관리합니다.

다이어리 세로 상품 목록 아래 `214개 상품 더보기` 버튼의 위쪽 여백은 16 기준 `1rem`입니다.

광고 `AD` 뱃지의 위쪽과 오른쪽 위치는 공통 페이지 여백 토큰을 사용합니다.

광고 문구는 24 기준 `1.5rem` 크기와 기본 `300` light를 사용하고, `빙수는` 강조부는 `600` semibold를 사용합니다. 왼쪽에는 배너 하단에 맞춘 `7.5rem` 정사각형 PNG 자리를 확보하고 위쪽 돌출을 허용하며, 돌출 영역의 최상단부터 이전 상품 섹션까지 `1.5rem` 간격을 유지합니다.

에어팟 케이스 큰 원형 상품은 중앙 스냅 캐러셀로 구현합니다. 초기 위치는 두 번째 상품이며 좌우 상품 일부를 함께 노출해 양방향 슬라이드임을 보여줍니다.

에어팟 케이스 큰 원형 상품 슬라이드의 상품 사이에는 24 기준 `1.5rem`의 고정 간격을 적용합니다. 제목과 슬라이드 사이는 공통 반응형 콘텐츠 간격을 유지합니다.

대표 추천 아래 원형 연관 상품 영역의 바깥 여백은 상·하·좌·우 모두 24 기준 `1.5rem`으로 고정합니다.

각 큐레이션 제목 오른쪽 원형 버튼은 `jt-kakao.svg`를 사용하는 카카오톡 공유 진입점입니다. 실제 공유 SDK 연동 전까지 버튼 UI와 접근 가능한 이름을 제공합니다.

광고 배너의 `AD` 배지 글자는 10 기준 `0.625rem` 예외 크기를 사용합니다.

일반 섹션의 판매처 원형은 향후 로고가 들어갈 표시용 뱃지로 구현합니다. 콘텐츠가 바뀌는 큐레이션 섹션의 원형도 시각적으로는 같은 뱃지 형태를 사용하며 선택 테두리와 링을 표시하지 않습니다. 탭 기능과 키보드 포커스 표시는 유지합니다.

`이 아이들 중 하나를 데려가렴` 섹션의 원형 상품 3개도 탭 버튼으로 동작합니다. 활성 탭 아래 연결 표시와 회색 상품 패널을 배치하고 선택에 따라 하단 상품 순서를 변경합니다.

회색 탭 패널 내부의 사각 상품 카드 사이는 12 기준 `0.75rem` 간격을 사용합니다.

원형 상품 탭의 이미지 영역만 지름 80 기준 `5rem`으로 고정합니다. 탭 행은 24 기준 `1.5rem` 간격을 둔 동일 너비 3열로 구성해 가용 너비의 양쪽 끝까지 사용하고, 상품명은 각 열 너비에 맞춥니다.

원형 상품 탭 아래 상품명은 12 기준 `0.75rem` 크기로 표시합니다.

`사러` 화면의 전체 타이포그래피는 초기 시안 값보다 2 기준씩 확대했습니다. 카드·캡션 14, 보조 제목 16, 섹션 제목 18, 상단 탭 26, 날씨 질문 30 기준을 공통 커머스 토큰으로 관리합니다.

앱 전체의 기본 폰트 굵기는 `300` light로 상속하며 활성 탭과 주요 제목 등 강조가 필요한 요소만 별도 굵기를 지정합니다.

앱 전체 기본 폰트 패밀리는 `Pretendard`를 최우선으로 사용합니다.

광고 아래 큐레이션 섹션에 표시되는 모든 상품명은 기본 굵기와 별개로 `400` regular를 사용합니다.

쇼핑 화면의 현재 판매 가격은 모두 `600` semibold를 사용하며, 취소선 정가는 `400` regular를 유지합니다.

상단 날씨 질문은 메인 헤드라인 예외로 30 기준 `1.875rem`을 사용하며, 첫 줄은 굵기 `600`, 둘째 줄은 굵기 `300`으로 표시합니다.

상단 `사러·먹으러·놀러` 탭은 26 기준 `1.625rem`으로 통일합니다. 활성 탭은 기본 글자색과 굵기 `600`, 비활성 탭은 캡션 색상과 굵기 `300`으로 구분합니다.

첫 진입 화면은 탭 순서를 바꾸지 않고 `사러`를 활성화합니다. 전체 레이아웃은 제공된 아이폰 17 시안의 상대 비율을 사용하며 다른 모바일 폭에서는 공통 유동 토큰으로 함께 축소·확대됩니다.

`먹으러` 메인 비주얼의 W 로고는 전체 형태가 읽히는 대형 크기와 오른쪽 정렬, 우측 크롭을 적용합니다. 상품 정보와 망고빙수 행 전체는 음수 여백으로 로고 비주얼 위에 겹쳐 불필요한 하단 공백 없이 이어집니다.

대표 상품 행은 로고 비주얼보다 높은 레이어를 사용해 겹치는 부분에서 상품이 항상 앞에 표시됩니다.

망고빙수 대표 상품은 투명 알파 PNG를 사용해 W 로고 위에 겹칠 때 원본의 흰 사각 배경이 노출되지 않도록 처리했습니다.

대표 상품 판매가와 정가는 flex 가격 행으로 묶어 행 단위 위쪽 여백, 간격과 baseline 정렬을 적용합니다.

대표 상품 정보와 상품 컷아웃은 2열 행의 세로 가운데를 기준으로 정렬합니다.

상단 `사러·먹으러·놀러` 탭의 글자 굵기 `650`을 앱 전체 타이포그래피의 상한으로 사용하며, 기존에 이보다 굵었던 텍스트도 같은 굵기로 통일했습니다.

전역 행간은 밀도 높은 모바일 UI를 위해 `1`을 기본값으로 사용하고, 여러 줄로 표시되는 제목과 본문에는 컴포넌트별 행간을 유지합니다.

첫 큐레이션 다음에는 참고 화면 순서대로 날씨 준비물 원형 상품 3개와 상품 3열, 큰 원형 비주얼 중심 상품, 단일 세로형 대표 상품, 2열 상품 그리드 섹션을 아래로 이어 붙입니다. 각 섹션은 흰색 카드이며 사이의 연한 회색 구분 영역으로 콘텐츠 묶음을 구분합니다.

`사러` 탭 대표 비주얼은 사용자가 제공한 `buy-sunny-w-transparent.png` 자체를 사용합니다. 실제 PNG는 `298 × 174`, 시안 프레임은 `316 × 174`이므로 오른쪽 빈 공간 없이 프레임 전체에 맞춰 표시합니다.

대표 W 이미지의 오른쪽 위치는 페이지 콘텐츠 여백을 상쇄해 화면 오른쪽 끝에 붙이고, 텍스트 영역은 기존 콘텐츠 여백을 유지합니다.

`사러` 탭은 오렌지 반소매 티셔츠를 메인 비주얼 오른쪽 아래에 겹치는 대표 상품으로 사용합니다. `놀러` 탭은 전용 에셋이 준비될 때까지 플레이스홀더를 유지합니다.

오렌지 반소매 티셔츠는 흰 배경이 포함된 RGB 원본 대신 배경을 제거한 투명 알파 PNG를 사용하며, 이미지 래퍼의 배경도 투명하게 유지합니다.

모든 탭의 대표 상품 정보와 이미지는 같은 2열 행에 배치합니다. 이미지는 오른쪽 열에서 위로 이동해 메인 비주얼과 겹치지만, DOM과 레이아웃상 상품 정보와 동일한 대표 상품 그룹을 유지합니다.

하단 고정 메뉴는 왼쪽부터 `쇼핑`, `날씨`, `마이페이지` 세 가지로 고정하며 현재 화면의 아이콘을 채움 상태로 강조합니다. 스크롤 방향과 관계없이 항상 화면 아래에 표시하고 본문 하단에 내비게이션 높이와 안전 영역만큼 여유를 둡니다.

하단 내비게이션의 기본 높이는 64 기준 `4rem`이며 기기 홈 인디케이터 영역은 `env(safe-area-inset-bottom)`으로 별도 확보합니다.

### 상품 상세 페이지

- 대표 이미지와 이미지 갤러리
- 상품명, 평점, 후기 수, 판매가와 할인 정보
- 날씨 적합도와 추천 이유
- 지역, 주소, 운영 시간과 이용 가능 날짜
- 옵션, 날짜, 회차, 인원과 수량 선택
- 남은 재고와 취소·환불 조건
- 상세 설명, 이용 안내, 판매자 정보와 후기
- 찜, 장바구니 담기와 바로 구매 버튼

### 장바구니 페이지

- 상품 선택, 옵션·수량 변경과 삭제
- 판매 종료, 재고 부족과 가격 변경 안내
- 상품 금액, 할인과 예상 결제 금액 계산
- 구매 가능한 항목만 주문서로 이동

### 주문서·결제 페이지

- 주문자와 실제 이용자 정보
- 예약 날짜·회차·인원 최종 확인
- 쿠폰과 포인트 적용
- 결제수단 선택과 필수 약관 동의
- 최종 결제 금액과 취소·환불 조건 확인
- 중복 결제를 막는 결제 진행 상태

### 주문 완료와 주문 관리 페이지

- 주문번호, 결제 결과와 이용 방법 표시
- 티켓 또는 예약 확인 정보 제공
- 주문 내역과 상세에서 이용 전·이용 완료·취소 상태 구분
- 정책에 따른 취소·환불 신청과 처리 결과 표시
- 결제 실패 시 장바구니 또는 주문서로 안전하게 복귀

## 핵심 날씨 페이지 상세 기준

## 1. 스플래시 페이지

현재 위치: `src/pages/SplashPage`

현재 상태: 구현 파일은 유지하되 쇼핑 UI 확인을 위해 `App.tsx` 진입 흐름에서는 임시로 연결을 해제했습니다. 이후 온보딩과 초기화 흐름을 구현할 때 다시 연결합니다.

공식 앱 로고는 `public/logos/weddy-logo.svg`를 사용하며 흰색 W와 노란 점의 고정 브랜드 색상 및 `1497:823` 비율을 유지합니다.

### 필수 구성

- Weather 로고
- SVG 캐릭터 또는 임시 캐릭터 장면
- 짧은 인트로 애니메이션
- 초기화 상태 문구
- 완료 후 다음 페이지로 이동하는 처리

### 확인할 상태

- 정상 로딩
- 캐릭터 에셋 로딩 실패
- 초기화가 오래 걸리는 경우
- 사용자가 모션 감소를 설정한 경우

### 완료 기준

- 모바일과 태블릿에서 레이아웃이 깨지지 않음
- 애니메이션 종료 또는 초기화 완료 후 정확히 한 번 이동함
- 네이티브 스플래시에서 React 화면으로 자연스럽게 연결됨

## 2. 날씨 홈 페이지

제안 위치: `src/pages/WeatherHomePage`

### 필수 구성

- 화면 전체를 감싸는 큰 흰색 라운드 카드
- 상단 왼쪽의 `오늘`, 날짜와 날짜 선택 버튼
- 상단 오른쪽의 공유와 지역 추가 버튼
- 큰 현재 온도, 어제 대비 온도와 최고·최저 온도
- 미세먼지와 초미세먼지 상태 배지
- 중앙의 날씨 아이콘, 현재 지역, 날씨 문구와 기준 시각
- 화면의 중심이 되는 우리 앱 전용 SVG 캐릭터 장면
- 현재 날씨에 맞는 소품, 바닥과 가벼운 장면 애니메이션
- 하단 가운데의 `날씨 더보기` 버튼과 예보 상세 이동 표시
- 하단 왼쪽의 상품 탐색 진입 버튼
- 하단 오른쪽의 마이페이지 진입 버튼

### 화면 배치 기준

```text
┌────────────────────────────┐
│ 오늘  날짜  ▼       공유  + │
│                            │
│ 현재 온도      미세 / 초미세 │
│ 최고·최저 온도              │
│                            │
│          날씨 아이콘         │
│     지역명 · 오늘의 날씨      │
│          기준 시각           │
│                            │
│       캐릭터와 날씨 장면       │
│                            │
│ 상품 탐색   날씨 더보기   MY  │
└────────────────────────────┘
```

참고 화면에서는 정보의 위계와 캐릭터 중심 구성을 참고하되, 타사의 캐릭터, 로고, 아이콘, 문구와 세부 그래픽은 사용하지 않습니다. 우리 로고, SVG 캐릭터, 날씨 아이콘과 컬러 토큰으로 별도의 화면을 제작합니다.

시간별·주간 예보의 긴 목록은 메인 화면에 모두 노출하지 않고 `날씨 더보기`를 통해 예보 상세 페이지에서 확인합니다. 상품 탐색 버튼은 `놀러·먹으러·보러` 허브 또는 전체 상품 목록으로 연결합니다.

### 먼저 사용할 목업 상태

- 맑음, 흐림, 비, 눈, 뇌우
- 낮과 밤
- `-12°C`, `38°C` 같은 극단 온도
- 긴 지역명
- 특보 없음, 특보 1개, 특보 여러 개

### 완료 기준

- 실제 API 형태와 비슷한 목업 데이터를 props로 전달함
- 날씨 데이터가 없어도 컴포넌트 자체가 깨지지 않음
- 모바일을 기본으로 하고 `48rem` 이상 태블릿 레이아웃을 지원함
- 상세 페이지로 이동할 수 있음
- 온도와 캐릭터 장면이 화면에서 가장 먼저 인지됨
- 상단 정보와 하단 내비게이션이 캐릭터 장면을 가리지 않음
- 공유, 지역 추가, 상품 탐색, 날씨 더보기와 마이페이지 버튼에 접근 가능한 이름이 있음

## 3. 위치 권한 안내 페이지

제안 위치: `src/pages/LocationPermissionPage`

### 필수 구성

- 현재 위치가 필요한 이유
- 위치 사용 허용 버튼
- 나중에 하기 또는 직접 지역 선택 버튼
- 개인정보와 위치 사용 범위에 대한 짧은 안내

### 확인할 상태

- 최초 요청 전
- 권한 허용
- 권한 거부
- 기기 위치 서비스 꺼짐
- 위치 조회 실패 또는 시간 초과

### 완료 기준

- 권한을 강요하지 않고 대체 흐름을 제공함
- 거부해도 기본 지역 또는 직접 선택으로 홈에 진입할 수 있음
- 같은 안내가 앱 실행마다 반복되지 않음

## 4. 예보 상세 페이지

제안 위치: `src/pages/ForecastPage`

### 필수 구성

- 날짜 또는 시간 범위 전환
- 시간별 온도, 강수 확률, 날씨 아이콘
- 일별 최고·최저 온도와 날씨 상태
- 선택한 날짜의 상세 정보
- 홈으로 돌아가기

### 확인할 상태

- 데이터 로딩
- 일부 시간대 데이터 누락
- 예보 데이터 전체 없음
- 네트워크 오류

### 완료 기준

- 가로 스크롤 영역을 키보드와 터치로 사용할 수 있음
- 단위와 날짜 표현이 일관됨
- 긴 목록에서도 현재 시각과 선택한 날짜를 쉽게 구분할 수 있음

## 5. 지역 관리 페이지

제안 위치: `src/pages/LocationsPage`

### 필수 구성

- 지역 검색 입력
- 검색 결과
- 현재 위치 항목
- 저장한 지역 목록
- 지역 추가, 삭제, 순서 변경 또는 선택

### 완료 기준

- 검색 결과 없음과 네트워크 오류를 구분함
- 현재 선택된 지역이 명확히 표시됨
- 삭제는 실수로 실행하지 않도록 확인 또는 되돌리기를 제공함

## 6. 설정 페이지

제안 위치: `src/pages/SettingsPage`

### 필수 구성

- 섭씨·화씨 선택
- 위치 권한 상태와 시스템 설정 이동 안내
- 모션 감소 설정
- 앱 버전과 데이터 출처

### 완료 기준

- 설정값이 앱 재실행 후에도 유지됨
- 설정 변경이 홈과 예보 화면에 즉시 반영됨

## 공통 상태 화면

각 페이지를 별도 디자인한 뒤 마지막에 추가하지 않고, 관련 페이지를 만들 때 함께 구현합니다.

| 상태           | 표시할 내용                 | 주요 행동      |
| -------------- | --------------------------- | -------------- |
| 로딩           | 스켈레톤 또는 진행 상태     | 기다리기       |
| 네트워크 오류  | 이해하기 쉬운 오류 문구     | 다시 시도      |
| 위치 권한 거부 | 현재 위치를 쓸 수 없는 이유 | 지역 직접 선택 |
| 빈 데이터      | 표시할 예보가 없음          | 새로고침       |
| 오래된 데이터  | 마지막 갱신 시각            | 다시 불러오기  |

## 권장 작업 단위

각 페이지는 아래 순서로 완성합니다.

1. 페이지의 목업 데이터 타입 정의
2. 모바일 정적 레이아웃 제작
3. 공통 UI를 컴포넌트로 분리
4. 태블릿 반응형 적용
5. 로딩, 오류, 빈 상태 추가
6. 접근성과 키보드 조작 확인
7. 실제 API 또는 기기 기능 연결
8. 린트, 빌드와 실제 화면 확인

## 현재 다음 작업

1. `SplashPage`의 임시 화면을 실제 인트로 디자인으로 정리
2. `WeatherHomePage` 폴더와 목업 날씨 데이터 타입 생성
3. 공통 상품 데이터 타입과 상품 카드 컴포넌트 정의
4. `ProductListPage`에서 놀러·먹으러·보러 목록 구현
5. `ProductDetailPage`에서 옵션·날짜·수량 선택 구현
6. 장바구니와 주문서의 목업 구매 흐름 연결
7. 실제 인증, 상품 API와 결제 서비스 선정 및 연동
