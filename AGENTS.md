# Weather App 작업 규칙

이 저장소를 수정하는 모든 작업은 다음 규칙을 따릅니다.

- 모든 SCSS 길이 값은 `rem`을 사용합니다. `px`은 사용하지 않습니다.
- `0`, `%`, `vw`, `vh`, `svh`, `dvh`, `em` 및 단위 없는 값처럼 의미상 필요한 값은 사용할 수 있습니다.
- 공통 크기, 여백, 타이포그래피 및 반응형 값은 `src/styles/tokens/_tokens.scss`에서 관리합니다.
- 공통 색상은 `src/styles/tokens/_color-tokens.scss`에서 관리합니다.
- UI는 컴포넌트 중심으로 작성하고 컴포넌트 전용 스타일은 해당 컴포넌트의 `*.module.scss`에서 관리합니다.
- 페이지에서만 필요한 배치와 변형은 해당 페이지의 `*.module.scss`에서 관리합니다.
- 기능, 구조, 의존성, 명령 또는 작업 규칙이 변경되거나 추가되면 같은 작업에서 `README.md`도 반드시 수정합니다.
- 작업 완료 전 SCSS의 `px` 사용 여부, 린트와 프로덕션 빌드를 확인합니다.
