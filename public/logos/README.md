# 로고 에셋

앱과 브랜드 화면에 사용하는 SVG 로고를 보관합니다.

- `weddy-logo.svg`: 현재 공식 Weddy 로고입니다. 흰색 W와 노란 점의 고정 색상 및 `1497:823` 비율을 유지합니다.
화면에서는 로고 파일을 직접 반복 참조하지 않고 `WeatherLogo` 컴포넌트를 사용합니다. 새 공식 로고로 변경할 때는 이 문서, 루트 `README.md`와 `WeatherLogo`의 소스 매핑을 같은 작업에서 함께 갱신합니다.
